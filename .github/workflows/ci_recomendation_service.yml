name: Recommendation Service CI/CD

on:
  pull_request:
    branches: [ main ]
    types:
      - opened
      - synchronize
      - closed

jobs:
  build-check:
    if: github.event.pull_request.merged == false
    runs-on: self-hosted
    steps:
      - name: Fix workspace permissions
        run: sudo chown -R $USER:$USER ${{ github.workspace }} || true

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build and test with coverage
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            gradle:8.14.3-jdk21-alpine \
            gradle --no-daemon test jacocoTestReport

      - name: Fix permissions after Docker
        run: sudo chown -R $USER:$USER ${{ github.workspace }}

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

  deploy:
    if: github.event.pull_request.merged == true
    runs-on: self-hosted
    steps:
      - name: Fix workspace permissions
        run: sudo chown -R $USER:$USER ${{ github.workspace }} || true

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set SHA tag
        id: vars
        run: echo "SHA=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GHCR_PAT }}" | \
          docker login ghcr.io -u ${{ secrets.GHCR_USERNAME }} --password-stdin

      - name: Build and push
        run: |
          docker build \
            -t ghcr.io/podcast-service/recommendation_service:${{ steps.vars.outputs.SHA }} \
            -t ghcr.io/podcast-service/recommendation_service:latest \
            .
          docker push ghcr.io/podcast-service/recommendation_service:${{ steps.vars.outputs.SHA }}
          docker push ghcr.io/podcast-service/recommendation_service:latest

      - name: Trigger deploy
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.DEPLOY_REPO_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/Podcast-service/Podcast_Service/dispatches \
            -d '{
              "event_type": "deploy",
              "client_payload": {
                "service": "recommendation_service",
                "image_tag": "${{ steps.vars.outputs.SHA }}"
              }
            }'