spring:
  application:
    name: recommendation-service
  datasource:
    url: ${RECOMMENDATION_DB_URL:jdbc:postgresql://localhost:${RECOMMENDATION_DB_PORT:15433}/recommendation_db}
    username: ${RECOMMENDATION_DB_USER:recommendation_user}
    password: ${RECOMMENDATION_DB_PASSWORD:recommendation_pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  flyway:
    enabled: ${RECOMMENDATION_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
    baseline-on-migrate: true
  kafka:
    bootstrap-servers: ${RECOMMENDATION_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    listener:
      ack-mode: manual_immediate
    consumer:
      group-id: ${RECOMMENDATION_KAFKA_CONSUMER_GROUP:recommendation-service}
      enable-auto-commit: false
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

server:
  port: ${RECOMMENDATION_SERVER_PORT:8083}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  api-docs:
    enabled: ${RECOMMENDATION_SWAGGER_ENABLED:false}
  swagger-ui:
    enabled: ${RECOMMENDATION_SWAGGER_ENABLED:false}
    path: /swagger
    disable-swagger-default-url: true
    tryItOutEnabled: true

app:
  kafka:
    topics:
      podcast-content-events: ${RECOMMENDATION_KAFKA_TOPIC_PODCAST_CONTENT_EVENTS:podcast.content.events.v1}
      podcast-activity-events: ${RECOMMENDATION_KAFKA_TOPIC_PODCAST_ACTIVITY_EVENTS:podcast.activity.events.v1}
  features:
    kafka-consumers-enabled: ${RECOMMENDATION_KAFKA_CONSUMERS_ENABLED:false}
    trends-api-enabled: ${RECOMMENDATION_TRENDS_API_ENABLED:true}
    personal-podcasts-api-enabled: ${RECOMMENDATION_PERSONAL_PODCASTS_API_ENABLED:true}
    refresh-job-enabled: ${RECOMMENDATION_REFRESH_JOB_ENABLED:false}
    global-job-enabled: ${RECOMMENDATION_GLOBAL_JOB_ENABLED:false}
    cache-cleanup-enabled: ${RECOMMENDATION_CACHE_CLEANUP_ENABLED:false}
  recommendation-cache:
    personal-ttl: ${RECOMMENDATION_PERSONAL_CACHE_TTL:PT30M}
    global-ttl: ${RECOMMENDATION_GLOBAL_CACHE_TTL:PT10M}
    refresh-user-limit: ${RECOMMENDATION_REFRESH_USER_LIMIT:100}
  jobs:
    recommendation-refresh-fixed-delay-ms: ${RECOMMENDATION_REFRESH_JOB_FIXED_DELAY_MS:1200000}
    global-recommendations-fixed-delay-ms: ${RECOMMENDATION_GLOBAL_JOB_FIXED_DELAY_MS:600000}
    cache-cleanup-fixed-delay-ms: ${RECOMMENDATION_CACHE_CLEANUP_FIXED_DELAY_MS:3600000}
